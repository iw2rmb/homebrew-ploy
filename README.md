# homebrew-ploy

Homebrew tap repository for `ploy`.

Install:

```bash
brew install --cask iw2rmb/ploy/ploy
```

Notes:
- This tap is updated by GoReleaser from `iw2rmb/ploy`.
- `Casks/ploy.rb` is currently a bootstrap placeholder until release assets are published.
